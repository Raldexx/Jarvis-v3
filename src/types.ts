// Shared types used across components

export interface ThemeConfig {
  bg: string;
  accent: string;
  accentSoft: string;
  cardBg: string;
  cardBorder: string;
  textPrimary: string;
  textMuted: string;
  sparkline: string;
  nowPlayingColor: string;
  banner: string;
  bannerBg: string;
  bannerBorder: string;
  bannerText: string;
  photo: string;
  photo2?: string;
}
